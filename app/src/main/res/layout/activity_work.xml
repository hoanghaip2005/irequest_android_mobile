<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#F5F5F5">

    <!-- Toolbar -->
    <androidx.appcompat.widget.Toolbar
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="?attr/colorPrimary"
        app:title="Quản lý yêu cầu"
        app:titleTextColor="@android:color/white" />

    <!-- TabLayout cho vai trò -->
    <com.google.android.material.tabs.TabLayout
        android:id="@+id/roleTabLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@android:color/white"
        app:tabMode="scrollable"
        app:tabTextColor="#757575"
        app:tabSelectedTextColor="#2196F3"
        app:tabIndicatorColor="#2196F3"
        app:elevation="4dp">

        <com.google.android.material.tabs.TabItem
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Yêu cầu của tôi" />

        <com.google.android.material.tabs.TabItem
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Công việc" />

        <com.google.android.material.tabs.TabItem
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Chưa gán" />

        <com.google.android.material.tabs.TabItem
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Phê duyệt" />

    </com.google.android.material.tabs.TabLayout>

    <!-- NavHostFragment để quản lý fragment navigation -->
    <androidx.fragment.app.FragmentContainerView
        android:id="@+id/nav_host_work"
        android:name="androidx.navigation.fragment.NavHostFragment"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        app:defaultNavHost="true"
        app:navGraph="@navigation/nav_graph_work" />

    <!-- Bottom Navigation chính -->
    <include layout="@layout/bottom_navigation" />

</LinearLayout>