<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:app="http://schemas.android.com/apk/res-auto">

    <!-- Style for feature cards on the home screen -->
    <style name="FeatureCardView" parent="Widget.MaterialComponents.CardView">
        <item name="cardCornerRadius">16dp</item>
        <item name="cardElevation">4dp</item>
        <item name="android:clickable">true</item>
        <item name="android:focusable">true</item>
        <item name="android:foreground">?android:attr/selectableItemBackground</item>
    </style>

    <!-- Style for the linear layout inside feature cards -->
    <style name="FeatureLayout">
        <item name="android:orientation">vertical</item>
        <item name="android:gravity">center</item>
        <item name="android:padding">16dp</item>
    </style>

    <!-- Style for the icon inside feature cards -->
    <style name="FeatureIcon">
        <item name="android:textSize">28sp</item> <!-- For emoji TextViews -->
        <item name="android:gravity">center</item>
    </style>

    <!-- Style for the title inside feature cards -->
    <style name="FeatureTitle" parent="Widget.AppCompat.TextView">
        <item name="android:textColor">?attr/colorOnSurface</item>
        <item name="android:textSize">12sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:gravity">center</item>
    </style>

    <!-- Styles for Performance Stat Cards -->
    <style name="StatCardView" parent="Widget.MaterialComponents.CardView">
        <item name="cardBackgroundColor">#E3F2FD</item>
        <item name="cardCornerRadius">12dp</item>
        <item name="cardElevation">0dp</item>
    </style>

    <style name="StatCardLayout">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:orientation">vertical</item>
        <item name="android:padding">12dp</item>
    </style>

    <!-- ĐÃ SỬA LỖI: Xóa bỏ parent không hợp lệ -->
    <style name="StatCardIcon">
        <item name="android:layout_width">24dp</item>
        <item name="android:layout_height">24dp</item>
        <item name="android:tint">?attr/colorPrimary</item>
    </style>

    <style name="StatCardTitle" parent="Widget.AppCompat.TextView">
        <item name="android:layout_marginTop">8dp</item>
        <item name="android:textSize">12sp</item>
        <item name="android:textColor">?attr/colorOnSurfaceVariant</item>
    </style>

    <style name="StatCardValue" parent="Widget.AppCompat.TextView">
         <item name="android:textSize">20sp</item>
         <item name="android:textStyle">bold</item>
         <item name="android:textColor">?attr/colorOnSurface</item>
    </style>

    <style name="StatCardTrend" parent="Widget.AppCompat.TextView">
        <item name="android:textSize">10sp</item>
    </style>

</resources>